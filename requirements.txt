# Multi-Agent Customer Support System - Requirements
# This project requires Python 3.9+ and uses only standard library
# No external dependencies needed for the core implementation

# Python Version
# python>=3.9

# Standard Library Modules Used:
# - asyncio (async/await for parallel execution)
# - json (data serialization)
# - typing (type hints)
# - dataclasses (data structures)
# - datetime (timestamps)
# - enum (enumeration for tool categories)
# - logging (comprehensive logging)
# - abc (abstract base classes)
# - uuid (unique identifiers)

# Optional: For production deployment
# If you want to extend with real APIs:
# requests>=2.28.0          # For HTTP API calls
# sqlalchemy>=1.4.0         # For database ORM
# python-dotenv>=0.20.0     # For environment variables
# pydantic>=1.9.0           # For data validation

# For monitoring:
# prometheus-client>=0.14.0 # Prometheus metrics
# python-json-logger>=2.0.0 # JSON logging

# For testing:
# pytest>=7.0.0             # Testing framework
# pytest-asyncio>=0.18.0    # Async test support

# For development:
# black>=22.0.0             # Code formatting
# flake8>=4.0.0             # Code linting
# mypy>=0.910               # Type checking
